FOUR_EYES_SUPREMACY

  # ECR_REPO: fleet-ecr
  # PROJECT: request-bin
  # SERVER_PATH: server/
  # SQL_FILE: server/db/migration.sql
  # BUILDER: paketobuildpacks/builder:base
  # DB_NAME: request_bin
  # SUBNET_NAME: Fleet-Subnet
  # SG_NAME: Fleet-Security-3
  # FS_TOKEN: FleetFS
  # REGION: us-east-2